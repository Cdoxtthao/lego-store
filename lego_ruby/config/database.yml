# config/database.yml
default: &default
  adapter: sqlserver
  host: localhost
  port: 1433
  database: LegoStoreDB
  integrated_security: true  

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default